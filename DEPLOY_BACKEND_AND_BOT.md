# Пошаговая инструкция по деплою бэкенда и Telegram бота

## 🎯 Цель
Задеплоить бэкенд (Flask API) и Telegram бота, чтобы они работали вместе с фронтендом на Vercel.

## 📋 Что нужно сделать

### 1. Деплой бэкенда на Railway (рекомендуется)

#### Шаг 1: Регистрация на Railway
1. Перейдите на [railway.app](https://railway.app)
2. Зарегистрируйтесь через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий `phoenix_lab`

#### Шаг 2: Настройка проекта
1. После подключения репозитория Railway покажет список сервисов
2. Нажмите на сервис (или создайте новый)
3. В настройках сервиса найдите **Root Directory**
4. Установите Root Directory: `Backend`
5. Railway автоматически определит Python приложение

#### Шаг 3: Добавление переменных окружения
В настройках сервиса (Settings → Variables) добавьте следующие переменные:

```bash
# Yandex API (обязательно)
YANDEX_CLOUD_API_KEY=AQVN2xArSNi6FoytO7KTX7OpSeG11H5jpEAXfDIN
YANDEX_CLOUD_PROJECT=b1goig30m707ojip72c7
YANDEX_CLOUD_ASSISTANT_ID=fvtfdp5dm8r044bnumjl

# Telegram Bot Token (обязательно)
BOT_TOKEN=8223416474:AAEr8DXOtlNzIR25B7vm3v37hTZPfPKw5BM

# Pexels API (для поиска изображений)
PEXELS_API_KEY=5NZHVjmbNSSA1CmlIMqY6dh9Nq33m3EXvRwEHu3mO0tZ7HdEayD2HtFt

# FusionBrain (Kandinsky) для генерации изображений
FUSIONBRAIN_API_KEY=FA70505746FFCD7BB7E2A6BF6DC392E6
FUSIONBRAIN_SECRET_KEY=FAD3C6ECAF2ECB4241425B30F192970A

# CORS - ВАЖНО! Замените на ваш Vercel URL
CORS_ORIGINS=https://your-app.vercel.app

# OpenRouter (если используется Qwen)
OPENROUTER_API_KEY=your_openrouter_key_here
```

**⚠️ ВАЖНО:** 
- Замените `https://your-app.vercel.app` на реальный URL вашего фронтенда на Vercel
- URL должен быть без завершающего слеша
- После добавления переменных Railway автоматически перезапустит сервис

#### Шаг 4: Получение URL бэкенда
1. После деплоя Railway создаст URL для вашего сервиса
2. URL будет выглядеть как: `https://your-app.railway.app`
3. **Сохраните этот URL** - он понадобится для настройки фронтенда

#### Шаг 5: Проверка работы бэкенда
1. Откройте URL бэкенда в браузере
2. Должна открыться страница с информацией о Flask API
3. Проверьте эндпоинт: `https://your-app.railway.app/api/channels`
4. Должен вернуться JSON с каналами

### 2. Обновление переменных окружения фронтенда

1. Перейдите на [vercel.com](https://vercel.com)
2. Откройте ваш проект
3. Перейдите в **Settings → Environment Variables**
4. Найдите или создайте переменную:
   - **Name**: `NEXT_PUBLIC_API_URL`
   - **Value**: URL вашего бэкенда (из шага 1.4)
   - **Environment**: Production, Preview, Development
5. Сохраните
6. Перезапустите деплой (Redeploy)

### 3. Деплой Telegram бота

Telegram бот должен работать постоянно, поэтому его нужно запустить на сервере.

#### Вариант A: Railway (рекомендуется)

1. В том же проекте Railway создайте **новый сервис**
2. Подключите тот же репозиторий
3. Установите **Root Directory**: `TelegramBot`
4. В настройках сервиса:
   - **Start Command**: `python main.py`
   - Или создайте `Procfile` в папке `TelegramBot`:
     ```
     worker: python main.py
     ```
5. Добавьте переменные окружения:
   ```bash
   BOT_TOKEN=8223416474:AAEr8DXOtlNzIR25B7vm3v37hTZPfPKw5BM
   ```
6. Railway автоматически запустит бота

#### Вариант B: Render

1. Перейдите на [render.com](https://render.com)
2. Создайте новый **Background Worker**
3. Подключите репозиторий
4. Настройки:
   - **Root Directory**: `TelegramBot`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
5. Добавьте переменные окружения (см. выше)
6. Нажмите "Create"

#### Вариант C: Локальный сервер / VPS

Если у вас есть VPS или вы хотите запустить бота локально:

1. Подключитесь к серверу
2. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/MerdonGG/phoenix_lab.git
   cd phoenix_lab/TelegramBot
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Создайте файл `.env`:
   ```bash
   BOT_TOKEN=8223416474:AAEr8DXOtlNzIR25B7vm3v37hTZPfPKw5BM
   ```
5. Запустите бота:
   ```bash
   python main.py
   ```
6. Для постоянной работы используйте `screen` или `systemd`:
   ```bash
   screen -S telegram_bot
   python main.py
   # Нажмите Ctrl+A, затем D для отсоединения
   ```

### 4. Проверка работы всей системы

#### Проверка бэкенда:
1. Откройте: `https://your-backend.railway.app/api/channels`
2. Должен вернуться JSON с каналами

#### Проверка фронтенда:
1. Откройте ваш Vercel URL
2. Попробуйте обработать статью
3. Проверьте, что запросы идут на правильный бэкенд (откройте DevTools → Network)

#### Проверка Telegram бота:
1. Откройте Telegram
2. Найдите вашего бота: `@PhoenixLogIN_bot`
3. Отправьте `/start`
4. Бот должен ответить

## 🔧 Troubleshooting

### Бэкенд не запускается
- Проверьте логи в Railway
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что Root Directory установлен как `Backend`

### CORS ошибки
- Убедитесь, что `CORS_ORIGINS` содержит ваш Vercel URL
- URL должен быть без завершающего слеша
- Перезапустите бэкенд после изменения переменных

### Telegram бот не отвечает
- Проверьте логи бота
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что бот запущен (не завершился с ошибкой)

### API запросы не работают
- Проверьте, что `NEXT_PUBLIC_API_URL` в Vercel указывает на правильный бэкенд
- Проверьте CORS настройки
- Откройте DevTools → Network и посмотрите на ошибки

## 📝 Чек-лист

- [ ] Бэкенд задеплоен на Railway
- [ ] Root Directory установлен как `Backend`
- [ ] Все переменные окружения добавлены в Railway
- [ ] `CORS_ORIGINS` содержит URL фронтенда
- [ ] Бэкенд отвечает на запросы
- [ ] `NEXT_PUBLIC_API_URL` обновлен в Vercel
- [ ] Telegram бот запущен
- [ ] Бот отвечает на команды
- [ ] Фронтенд работает с бэкендом

## 🎉 Готово!

После выполнения всех шагов ваша система должна работать:
- ✅ Фронтенд на Vercel
- ✅ Бэкенд на Railway
- ✅ Telegram бот работает

Если что-то не работает, проверьте логи на соответствующих платформах.

