# Phoenix Lab

<div align="center">
  <img src="Frontend/public/logo.png" alt="Phoenix Lab Logo" width="200" />
</div>

**Phoenix Lab** — веб-приложение для AI-рерайта статей с поддержкой двух провайдеров (Qwen и YandexGPT), интеграцией Telegram для рассылки и авторизацией через Telegram бота.

## 🚀 Возможности

- **AI-рерайт статей** с выбором стиля (научно-деловой, мемный, повседневный)
- **Два провайдера AI**: Qwen (OpenRouter) и YandexGPT
- **Авторизация через Telegram** бота
- **Рассылка статей** в Telegram каналы
- **Управление каналами** через Telegram бота
- **Темная/светлая тема** с сохранением настроек
- **Анимация логотипа** при клике
- **Адаптивный дизайн** для всех устройств

## 📋 Структура проекта

```
phoenix_lab/
├── Frontend/              # Next.js Frontend
│   ├── app/              # App Router (React компоненты)
│   │   ├── page.tsx      # Главная страница
│   │   ├── components/   # React компоненты
│   │   │   └── AuthMenu.tsx  # Меню авторизации
│   │   └── globals.css   # Глобальные стили
│   ├── public/           # Статические файлы
│   │   ├── logo.png      # Логотип
│   │   └── assets/       # Анимации и медиа
│   │       ├── горение.gif
│   │       └── горение.mp4
│   ├── package.json
│   └── next.config.js
├── Backend/              # Backend компоненты
│   ├── server.py         # Flask API сервер
│   ├── requirements.txt  # Python зависимости
│   ├── openrouter.env    # Настройки OpenRouter (Qwen)
│   └── yandex.env        # Настройки YandexGPT
├── TelegramBot/          # Telegram бот
│   ├── main.py           # Основной файл бота
│   ├── requirements.txt  # Python зависимости
│   ├── channels.json     # Файл с каналами (создаётся автоматически)
│   └── auth_tokens.json  # Токены авторизации (создаётся автоматически)
└── README.md
```

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd phoenix_lab
```

### 2. Backend установка

```bash
# Установка зависимостей
pip install -r Backend/requirements.txt
pip install -r TelegramBot/requirements.txt
```

### 3. Настройка переменных окружения

#### Основные настройки (корень проекта)

Создайте файл `.env` или `BOT_TOKEN.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token_here
PORT=5000
API_URL=http://localhost:5000
```

#### Настройка Qwen (OpenRouter)

Создайте файл `Backend/openrouter.env`:

```env
OPENROUTER_API_KEY=sk-or-v1-your_api_key_here
OPENROUTER_MODEL=qwen/qwen2.5-72b-instruct
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
```

**Как получить API ключ OpenRouter:**
1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Перейдите в раздел **Keys**
3. Создайте новый ключ
4. Скопируйте ключ (формат: `sk-or-v1-...`)

Подробнее: [OPENROUTER_SETUP.md](OPENROUTER_SETUP.md)

#### Настройка YandexGPT (опционально)

Создайте файл `Backend/yandex.env`:

```env
YANDEX_CLOUD_API_KEY=your_yandex_cloud_api_key_here
YANDEX_CLOUD_PROJECT=your_yandex_cloud_project_id_here
YANDEX_CLOUD_ASSISTANT_ID=your_yandex_cloud_assistant_id_here
```

**Как получить ключи YandexGPT:**
1. Зарегистрируйтесь в [Yandex Cloud](https://cloud.yandex.ru/)
2. Создайте сервисный аккаунт
3. Назначьте роль `ai.languageModels.user`
4. Создайте API ключ для сервисного аккаунта
5. Создайте проект и получите Project ID
6. Создайте ассистента в YandexGPT и получите Assistant ID

### 4. Frontend установка

```bash
cd Frontend
npm install
# или
yarn install
```

Создайте файл `Frontend/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

### 5. Копирование медиа файлов

Скопируйте файлы анимации из `phoenix_lab_yandex/горение/` в `Frontend/public/assets/`:
- `горение.gif` → `Frontend/public/assets/горение.gif`
- `горение.mp4` → `Frontend/public/assets/горение.mp4`

## 🎯 Запуск

### 1. Запуск Backend сервера

```bash
cd Backend
python server.py
```

Сервер запустится на `http://localhost:5000`

### 2. Запуск Telegram бота

В отдельном терминале:

```bash
cd TelegramBot
python main.py
```

Бот начнет обрабатывать команды и авторизацию.

### 3. Запуск Frontend

В отдельном терминале:

```bash
cd Frontend
npm run dev
# или
yarn dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📖 Использование

### На сайте

1. **Введите URL статьи** в поле ввода
2. **Выберите стиль рерайта**:
   - Научно-деловой стиль
   - Мемный стиль
   - Повседневный стиль
3. **Выберите провайдера AI**:
   - **Qwen (OpenRouter)** — рекомендуется для большинства случаев
   - **YandexGPT** — альтернативный вариант
4. Нажмите **"Рерайт статьи"**
5. После обработки нажмите **"Telegram"** для отправки в каналы
6. Выберите каналы и отправьте статью

### Авторизация через Telegram

1. Нажмите кнопку **"🔐 Войти"** в правом верхнем углу
2. Откроется меню авторизации
3. Нажмите **"🔐 Открыть бота для авторизации"**
4. В Telegram откройте бота `@phoenixllab_bot`
5. Нажмите кнопку **"🔐 Авторизоваться на сайте"**
6. Авторизация произойдет автоматически

### Управление каналами через бота

Команды бота:
- `/start` — начать работу
- `/channels` — показать список каналов
- `/add_channel` — добавить новый канал
- `/help` — помощь по командам

**Добавление канала:**
1. Добавьте бота в канал как администратора
2. Используйте команду `/add_channel`
3. Перешлите сообщение из канала или отправьте ID канала

## 🤖 Провайдеры AI: Qwen vs YandexGPT

Проект поддерживает два провайдера для рерайта статей. Каждый имеет свои особенности:

### Qwen (OpenRouter)

**Технология:** Qwen 2.5 72B через OpenRouter API

**Преимущества:**
- ✅ Высокое качество рерайта
- ✅ Отличное понимание контекста
- ✅ Поддержка различных стилей
- ✅ OpenAI-совместимый API
- ✅ Прозрачное ценообразование
- ✅ Надежная инфраструктура

**Особенности реализации:**
- Использует системный промпт с четкими правилами
- Параметры генерации: `temperature=0.5`, `max_tokens=4000`, `top_p=0.95`
- Стоп-последовательности для предотвращения "мыслей" модели
- Автоматическая очистка ответа от лишних комментариев

**Рекомендуется для:**
- Большинства случаев использования
- Когда нужен качественный рерайт
- Когда важна стабильность работы

**Настройка:**
```env
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=qwen/qwen2.5-72b-instruct
```

### YandexGPT

**Технология:** YandexGPT через Yandex Cloud API

**Преимущества:**
- ✅ Хорошая поддержка русского языка
- ✅ Интеграция с экосистемой Yandex
- ✅ Возможность использования российских серверов
- ✅ Простая настройка через Yandex Cloud

**Особенности реализации:**
- Использует Yandex Cloud Assistant API
- Более простой промпт с акцентом на русский язык
- Требует настройки Project ID и Assistant ID
- Автоматическая очистка результата от мыслей модели

**Рекомендуется для:**
- Когда нужна альтернатива Qwen
- Когда важна локализация в России
- Когда требуется использование российских серверов

**Настройка:**
```env
YANDEX_CLOUD_API_KEY=your_api_key
YANDEX_CLOUD_PROJECT=your_project_id
YANDEX_CLOUD_ASSISTANT_ID=your_assistant_id
```

### Сравнительная таблица

| Параметр | Qwen (OpenRouter) | YandexGPT |
|----------|-------------------|-----------|
| **Модель** | Qwen 2.5 72B | YandexGPT |
| **API формат** | OpenAI-совместимый | Yandex Cloud API |
| **Качество рерайта** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Скорость** | Быстро | Средне |
| **Поддержка русского** | Отлично | Отлично |
| **Сложность настройки** | Простая | Средняя |
| **Стоимость** | Прозрачная | Зависит от тарифа |
| **Доступность** | Глобально | Россия/СНГ |

### Когда использовать какой провайдер?

**Используйте Qwen, если:**
- Нужно максимальное качество рерайта
- Важна стабильность и надежность
- Работаете с международной аудиторией

**Используйте YandexGPT, если:**
- Нужна альтернатива Qwen
- Важна локализация в России
- Требуется использование российских серверов

**Можно использовать оба:**
- Проект поддерживает переключение между провайдерами
- Можно сравнить результаты и выбрать лучший для конкретной задачи

## 🔌 API Endpoints

### Backend API

- `GET /api/health` — проверка работоспособности сервера
- `GET /api/channels` — получить список каналов
- `POST /api/rewrite-article` — рерайтить статью
  ```json
  {
    "url": "https://example.com/article",
    "style": "scientific|meme|casual",
    "provider": "qwen|yandex"
  }
  ```
- `POST /api/send-article` — отправить статью в каналы
  ```json
  {
    "article_text": "текст статьи",
    "channels": ["channel_id1", "channel_id2"]
  }
  ```

### Авторизация API

- `POST /api/auth/generate-token` — сгенерировать токен авторизации
- `POST /api/auth/verify-token` — проверить токен
  ```json
  {
    "token": "token_string"
  }
  ```
- `POST /api/auth/authorize` — авторизовать токен (вызывается ботом)
  ```json
  {
    "token": "token_string",
    "user_data": {
      "id": 123456,
      "first_name": "Имя",
      "username": "username"
    }
  }
  ```

## 🎨 Стили рерайта

### Научно-деловой стиль
- Формальный тон
- Объективность
- Научная терминология
- Структурированность

### Мемный стиль
- Интернет-мемы и эмодзи
- Сленг и разговорные выражения
- Сарказм и юмор
- Неформальный тон

### Повседневный стиль
- Простота и понятность
- Естественность
- Разговорный тон
- Доступность для широкой аудитории

## 🛠️ Технологии

### Frontend
- **Next.js 14** — React фреймворк
- **TypeScript** — типизация
- **CSS Modules** — стилизация
- **LocalStorage** — сохранение настроек

### Backend
- **Flask** — веб-фреймворк
- **aiogram** — Telegram Bot API
- **BeautifulSoup4** — парсинг HTML
- **requests** — HTTP запросы
- **openai** — клиент для YandexGPT (совместимость)

### AI Провайдеры
- **OpenRouter** — доступ к Qwen моделям
- **Yandex Cloud** — YandexGPT API

## 📝 Лицензия

Проект создан для внутреннего использования.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи backend сервера
2. Проверьте логи Telegram бота
3. Убедитесь, что все переменные окружения настроены
4. Проверьте подключение к API провайдеров

## 📚 Дополнительная документация

- [Настройка OpenRouter API](OPENROUTER_SETUP.md)
- Документация по настройке YandexGPT доступна в папке `phoenix_lab_yandex/`

---

<div align="center">
  <p>Сделано с ❤️ для Phoenix Lab</p>
</div>
